"""Package initializer for the refactored barbershop application.

This file can remain empty but indicates to Python that
`barberia_refactored` is a package. Having a package allows relative
imports between modules (e.g. `from .database import ...` if this
package were imported elsewhere).
"""